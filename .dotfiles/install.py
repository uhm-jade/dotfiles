# TODO: get an argument for "don't overwrite files" i think this is called merge
# And use something like this
# if dest.exists():
#     print("File already exists — skipping:", dest)
# else:
#     shutil.copy2(src, dest)
